class Node {
    constructor(val) {
        this.val = val;
        this.next = null;
    }
}

class SinglyLinkedList {
    constructor() {
        this.head = null;
        this.tail = null;
        this.length = 0;
    }
    push(val) {
        if (this.head === null) {
            this.head = val;
            this.tail = val;
            length++;
        } else if (this.head == this.tail) {
            this.tail = val;
            length++;
        } else {
            this.
        }
    }
}